Import('env')

includes = [
    'include',
    'include/cmsis'
]

sources = [
    'error/nrf_fault.c',
    'error/nrf_assert.c',
    'error/nrf_error.c',
]

env.Append(CPPPATH=includes)

prg = env.Library(
    target = 'core',
    source = sources
)
